<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>
  <td><%= button_to t('.buttons.start'), start_test_path(test), class: 'btn btn-info' %></td>
</tr>