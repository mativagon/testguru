<h1><%= t('.header', title: @question.test.title) %></h1>

<p><%= @question.body %></p>

<h2><%= t('.variants') %></h2>

<ol><%= render @question.answers %></ol>

<%= link_to t('.links.new'), new_admin_question_answer_path(@question) %> |
<%= link_to t('.links.back'), admin_test_path(@question.test) %>