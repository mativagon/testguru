<div class="nav nav-admin">
  <span class="nav-admin title">TestGuru <%= t('.admin_panel') %></span>

  <div class="nav-admin user">
    <%= t('.welcome') %>, <%= current_user.first_name %>!
    <%= link_to t('.links.logout'), destroy_user_session_path, method: :delete %>
  </div>
</div>